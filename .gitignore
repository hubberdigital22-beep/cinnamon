# Originais do cliente (.ai, .pdf, .rar, PNGs 4k) — ~344 MB.
# Ficam só no disco local: o site consome as versões já processadas em img/.
# Para regenerar img/ a partir deles: ./scripts/prepare-assets.sh all
Assets/

# arquivos obsoletos marcados para remoção manual
_to_delete/

# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?

# editores
.vscode/
.idea/
*.swp

# node (usado só para ferramentas pontuais, nunca em runtime)
node_modules/

# Copy deck e notas internas — NUNCA no repositório (este repo é público).
# Contêm o "nunca publicar", a análise de exposição jurídica e os dados
# marcados citavel:false. O .vercelignore já os tira do deploy; isto os
# tira também do GitHub. Fluxo previsto: gerar o HTML local e versionar
# só o HTML pronto (ver content/README.md).
content/
scripts/deck/
__pycache__/
